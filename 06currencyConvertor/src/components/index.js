// import all components here
import InputBox from "./Inputbox";

// then export all components 
export {InputBox}

// now we just need to import only one file (this file) and we can access all components from it